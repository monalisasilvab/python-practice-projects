# Hangman

A simple command-line Hangman game written in Python. Guess letters to reveal a hidden word before you run out of lives.

## Features

- Single-player, terminal-based gameplay
- Uses a predefined word list (hangman_words.py)
- Input validation (repeated guesses are handled)
- Lives counter with visual stages