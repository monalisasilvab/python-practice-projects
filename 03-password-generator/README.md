# Password Generator

A small, interactive Python script that creates randomized passwords based on user-specified counts of letters, symbols and numbers. It builds a character list, shuffles it and returns a final password string.

## Features

- Choose how many letters, symbols and numbers 
- Random selection from predefined character sets
- Shuffling to avoid predictable grouping of characters
- Simple, zero-dependency script