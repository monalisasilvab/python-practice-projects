# Simple Calculator

A minimal command-line calculator that supports addition, subtraction, multiplication and division. Displays an ASCII logo and lets you chain calculations: continue using the last result or start a new calculation.

## Features
- Supports +, -, *, /
- Continue calculating with the previous result (accumulate) or start over
- Simple, interactive CLI